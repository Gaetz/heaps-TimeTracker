package ui;

@:uiComp("layoutCol")
class LayoutColComp extends h2d.Flow implements h2d.domkit.Object {

    static var SRC = <layoutCol>
        
    </layoutCol>;

    public function new(align:h2d.Flow.FlowAlign, ?parent) {
        super(parent);
        initComponent();
    }
}